{
    "name": "networkteam/typo3-filesystem-garbage-collection",
    "description": "Filesystem garbage collection",
    "type": "typo3-cms-extension",
    "license": [
        "GPL-2.0-or-later"
    ],
    "require": {
        "typo3/cms-core": "^11.5 || ^12.4 || ^13.0",
        "php": ">=8.0"
    },
    "autoload": {
        "psr-4": {
            "Networkteam\\FilesystemGarbageCollection\\": "Classes/"
        }
    },
    "extra": {
        "typo3/cms": {
            "extension-key": "networkteam_filesystem_garbage_collection"
        }
    }
}